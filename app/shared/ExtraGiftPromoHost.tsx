"use client";

/** Modal de cotas grátis desativado — mantém o wrapper para compatibilidade de imports. */
export function ExtraGiftPromoHost({ children }: { children: React.ReactNode }) {
  return children;
}
