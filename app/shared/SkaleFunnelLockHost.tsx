"use client";

/**
 * Lock do funil Skale DESATIVADO — não redireciona mais a navegação para /skale.
 * (Mantido como no-op para não quebrar imports/montagem em providers.)
 */
export function SkaleFunnelLockHost() {
  return null;
}
